# Neon Postgres connection string — required in production (Vercel).
# Neon dashboard → your project → "Connect" → copy the pooled connection string.
# Locally you can leave this unset: the app falls back to a JSON-file store
# in ./data (single-instance dev only).
DATABASE_URL=
